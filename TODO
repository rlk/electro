grab command line flag
Galaxy file byte order?
Windows DND batch
function pointers?

gl state init
stereo visual
object geometry create
bounding sphere
tracker button
text render of some sort / TTF
network
camera offset per tile (for geowall and planar)

Test whether tracking data optimization will pay off.
Scale and billboard frustum (thorough test)
camera near/far (automatic using entity bounding sphere?))

Particle system
